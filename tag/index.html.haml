---
layout: standard
---
%h2
  Posts Tagged '#{page.tag}'
- if page.pages
  - page.pages.each do |post|
    .post.hentry
      %h2
        %a{:href=>post.url}= post.title
      - if post.date && post.categories
        %p.postmetadata
          Posted
          - if post.date
            = post.date.strftime('%Y-%m-%d')
          - if post.respond_to?(:category_links)
            in
            = post.category_links(:delimiter => " » ") 

      %p.postmetadata
        Tags:
        = post.tag_links

      .entry
        - if post.summary
          = post.summary
          %p
            %a{:href => post.url} Read More »
        -else
          = post.content
